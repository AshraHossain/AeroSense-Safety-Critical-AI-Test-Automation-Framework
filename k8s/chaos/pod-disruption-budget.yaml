apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: aerosense-pdb
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: aerosense
      component: testforge
