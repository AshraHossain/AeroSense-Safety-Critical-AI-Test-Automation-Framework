# Staging environment overrides

replicaCount: 2

image:
  tag: staging

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

config:
  LOG_LEVEL: info

postgres:
  replicas: 1
  storage: 5Gi
